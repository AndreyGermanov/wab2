# Файл со списком зон основного домена.
#
# Обычно это одна прямая и одна обратная зоны.
# Файл генерируется автоматически панелью управления.
# Его не нужно менять (и смысла нет. Если хотите обслуживать еще одну зону,
# создайте такой же файл, заполните его и добавьте в список в конце файла 
# name.conf)

zone "{zone_name}" {
    type master;
    file "/etc/bind/{file}";
    allow-update { key DHCP_UPDATE; };
};